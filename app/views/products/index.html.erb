<div id="products-page">
  <div id="modal-overlay" class="modal-overlay" data-controller="preorder-modal" data-preorder-modal-target="overlay">
    <div class="modal-content">
      <button class="close-modal" data-action="preorder-modal#close">✕</button>
      <h6>PRE-ORDER NOW OPEN</h6>
      <h6>AVAILABLE UNTIL 30TH OF NOVEMBER</h6>
      <p>WELCOME TO THE LATEST CHAPTER OF SHERS STUDIOS - NAME THE UNNAMED. EACH PRIECE IN THIS COLLECTION IS AVAILABLE FOR PRE-ORDERS ONLY.</p>
      <p>ONCE THE PRE-ORDER PERIOD CLOSES, PRODUCTION WILL BEGIN, WITH CRAFTING TIMES RANGING FROM 7 TO 30 WORKING DAYS, DEPENDING ON OUR CURRENT DEMAND.</p>
    </div>
  </div>
  <% if current_user&.admin? %>
    <%= link_to new_product_path, class: "centered-link" do %>
      <i class="fa-solid fa-plus"></i>
    <% end %>
  <% end %>

  <div class="product-grid">
    <% @products.each do |product| %>
      <div class="product-card">
        <% if current_user&.admin? %>
          <%= link_to edit_product_path(product), class: "edit-link" do %>
            <i class="fa-solid fa-edit"></i>
          <% end %>
        <% end %>

        <%= link_to product_path(product), class: "product-card-link" do %>
          <%= image_tag product.primary_photo %>
          <div class="product-card-info">
            <div>
              <%= product.name.upcase %><br>
              <%= humanized_money_with_symbol(product.price) %>
            </div>
            <% if user_signed_in? %>
              <button
                data-controller="wishlist"
                data-wishlist-product-id-value="<%= product.id %>"
                data-action="click->wishlist#toggle"
                class="wishlist-button">
                <i class="<%= current_user.wishlist&.products&.include?(product) ? 'fa-solid fa-heart' : 'fa-regular fa-heart' %>"></i>
              </button>
            <% end %>
          </div>
        <% end %>

      </div>
    <% end %>
  </div>
</div>
